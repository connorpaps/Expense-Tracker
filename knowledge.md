## Final category-preview runtime hardening — 2026-08-06

- Repository SQLite boolean mapping now accepts numeric, string, and boolean forms for vault/category/rule flags, preventing adapter-specific `is_active` values from suppressing automatic suggestions.
- Development startup unregisters old service workers and removes only `expense-tracker-shell-*` caches, preventing stale production shells from masking current Vite source on localhost without touching vault data.
- Final focused validation passed 24 tests; the full repository gate passed 43 files / 211 tests, all workspace typechecks, lint, production build, and diff check before the final defensive refinements. Fresh live TD audit passed 23/23 after the refinements, with all 19 category selects populated, zero legacy fallback messages, and no console errors.
- Development cache cleanup is scoped to this app's same-origin `/sw.js` registration and `expense-tracker-shell-*` caches only. Encrypted import mutation payloads use corrected category IDs when the user changes a suggestion.
- Open issue at session close: the user's real browser still shows blank TD category selects and the legacy fallback message, while fresh automated Chrome profiles pass 23/23 with 19/19 selected categories. Next debugging must instrument the actual tab's loaded bundle, service-worker state, category rows, and preview object rather than relying on clean-profile evidence alone.
