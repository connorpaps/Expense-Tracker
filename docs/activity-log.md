# Activity Log (auto-generated)

This file is appended automatically by the git `post-commit` hook (`.githooks/post-commit`)
after every commit. It records the date, commit message, and changed files so future sessions
can see what changed without being told.

Raw per-save events, if the optional watcher is running, go to `docs/activity-watch.log` (gitignored).
